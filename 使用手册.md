# 规则引擎使用手册

基于开源 [GoRules ZEN Engine](https://github.com/gorules/zen) 的决策规则系统。
业务人员在**可视化编辑器**中维护规则（保存为 JSON 文件），**Python 引擎**加载同一份文件执行求值。

---

## 目录

1. [系统架构](#1-系统架构)
2. [目录结构](#2-目录结构)
3. [环境要求与安装](#3-环境要求与安装)
4. [启动与停止](#4-启动与停止)
5. [可视化编辑器使用](#5-可视化编辑器使用)
6. [ZEN 表达式语言要点](#6-zen-表达式语言要点)
7. [Python 引擎使用](#7-python-引擎使用)
8. [典型工作流：改一条规则并上线](#8-典型工作流改一条规则并上线)
9. [常见问题 FAQ](#9-常见问题-faq)

---

## 1. 系统架构

```
┌─────────────────────┐         ┌──────────────────────┐
│  可视化编辑器         │         │  Python 引擎服务      │
│  (浏览器 :8700)      │         │  (FastAPI :8800)     │
│                     │         │                      │
│  · 画布编排节点       │         │  · REST 求值 API     │
│  · 决策表/表达式编辑  │         │  · CLI / 库调用      │
│  · 模拟器(轨迹回放)   │         │  · 微秒级求值        │
└─────────┬───────────┘         └──────────▲───────────┘
          │ 保存 .json                     │ 读取
          ▼                                │
      ┌─────────────────────────────────────┘
      │   rules/*.json   ← 规则唯一事实来源（JDM 格式）
      └────────────────────────────────────
```

核心设计：

- **规则即文件**：所有决策规则以 JDM（JSON Decision Model）格式存放在 `rules/` 目录，可进 Git 做版本管理和 diff 审查
- **单一事实来源**：编辑器保存的文件 = Python 引擎执行的文件，两边是同一个 Rust 引擎内核的不同语言绑定，求值结果一致
- **编辑器内置模拟器**：求值由 Node 侧 `@gorules/zen-engine` 完成，带 trace 返回，画布会高亮执行路径

## 2. 目录结构

```
规则引擎/
├── 使用手册.md             ← 本文档
├── README.md              ← 项目简介
├── rules/                 ← 规则文件目录（唯一事实来源）
│   ├── shipping-fee.json      示例：运费计算（决策表 + 表达式）
│   └── customer-score.json    示例：客户评分（决策表 + Switch 分流）
├── editor/                ← 可视化编辑器
│   ├── server.mjs             Node 服务（文件 CRUD + 模拟求值 API，端口 8700）
│   ├── src/App.tsx            编辑器界面源码
│   ├── dist/                  构建产物（npm run build 生成）
│   └── package.json
└── engine/                ← Python 引擎
    ├── app.py                 FastAPI 服务（端口 8800）
    ├── run.py                 命令行求值工具
    ├── demo.py                库调用示例
    └── requirements.txt
```

## 3. 环境要求与安装

### 环境要求

| 软件 | 版本 | 用途 |
|---|---|---|
| Node.js | ≥ 18 | 编辑器前端构建 + 模拟器服务端 |
| Python | ≥ 3.10 | 引擎服务 |
| 网络 | 可访问 npm/PyPI | 安装依赖（国内建议配置镜像源） |

### 首次安装

```powershell
# 编辑器依赖（在 editor/ 目录）
cd editor
npm install
npm run build

# Python 引擎依赖（在 engine/ 目录）
cd ..\engine
pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
```

> 提示：若本机 pip 配置了内部镜像源且缺少 `zen-engine` 包，务必加 `-i https://mirrors.aliyun.com/pypi/simple/`（或清华源 `https://pypi.tuna.tsinghua.edu.cn/simple/`）。

## 4. 启动与停止

### 启动编辑器（端口 8700）

```powershell
cd editor
npm start                       # 前台运行，Ctrl+C 停止
```

浏览器打开 **http://localhost:8700**。

开发模式（修改编辑器源码时用，支持热更新）：

```powershell
# 终端 1
npm run server                  # 后端 API，端口 8700
# 终端 2
npm run dev                     # vite 开发服务器，端口 5173（已代理 /api）
```

### 启动 Python 引擎（端口 8800）

```powershell
cd engine
uvicorn app:app --host 0.0.0.0 --port 8800    # 前台运行，Ctrl+C 停止
```

### 后台运行 / 停止（Windows PowerShell）

```powershell
# 后台启动编辑器服务
Start-Process node -ArgumentList "server.mjs" -WorkingDirectory "C:\path\to\规则引擎\editor" -WindowStyle Hidden

# 查看并停止服务
Get-Process node | Where-Object {$_.MainWindowTitle -eq ''}   # 找到进程后 Stop-Process -Id <PID>
```

### 端口约定

| 端口 | 服务 |
|---|---|
| 8700 | 编辑器（界面 + 文件 API + 模拟求值 API） |
| 8800 | Python 引擎 REST 服务 |

## 5. 可视化编辑器使用

打开 http://localhost:8700 后界面分三部分：

```
┌──────────────────────────────────────────────────┐
│ 顶栏：规则文件选择 | 保存 | 新建 | 删除 | 模式切换   │
├────────┬─────────────────────────┬───────────────┤
│ 左侧    │                         │ 右侧           │
│ 节点面板 │       画布（拖拽编排）     │ 模拟器面板      │
└────────┴─────────────────────────┴───────────────┘
```

### 5.1 画布基本操作

| 操作 | 方式 |
|---|---|
| 添加节点 | 从左侧节点面板拖入画布，或双击节点类型 |
| 连线 | 从节点右侧圆点拖到下一节点左侧圆点 |
| 打开节点编辑 | 双击画布上的节点（在下方标签页中编辑） |
| 删除节点/边 | 选中后按 Delete |
| 移动画布 | 空白处拖拽 / 滚轮缩放 |

### 5.2 节点类型

| 节点 | 作用 | 关键配置 |
|---|---|---|
| **请求（inputNode）** | 决策入口，定义输入数据 | content.schema 可选填 JSON Schema |
| **决策表（decisionTableNode）** | 电子表格式条件→结果，最常用 | hitPolicy: `first`（命中首行）/ `collect`（收集所有命中行） |
| **表达式（expressionNode）** | 计算/转换字段 | expressions: key-value 表达式列表 |
| **Switch（switchNode）** | 条件分流 | statements: 多条件分支，可设默认分支 |
| **函数（functionNode）** | 写 JS 处理复杂逻辑（支持 http/dayjs 等模块） | source: JS 代码 |
| **子决策（decisionNode）** | 引用另一个规则文件，实现复用 | content.key: 如 `shipping-fee.json` |
| **响应（outputNode）** | 决策出口，返回结果 | content.schema 可选 |

### 5.3 决策表编辑

双击决策表节点打开表格视图：

- **输入列**：点击表头设置字段路径（如 `customer.tier`）和名称；单元格填 ZEN 一元表达式（如 `"vip"`、`>= 500`、`[20..40]`），**留空表示任意值**
- **输出列**：点击表头设置输出字段（如 `discount`）；单元格填输出值表达式（如 `0.15`、`"gold"`）
- **规则行**：从上到下依次匹配，`hitPolicy: first` 时命中一行即停止
- **描述列**：`_description` 可写业务备注，不影响执行

### 5.4 模拟器（重点功能）

右侧面板（默认已打开，若没有请点击右侧工具条按钮）：

1. 在 JSON 输入框填写测试数据，如：
   ```json
   {
     "customer": { "tier": "vip", "years": 3 },
     "order": { "total": 800 },
     "cart": { "weight": 12 }
   }
   ```
2. 点击 **Run**
3. 效果：
   - 画布上**走过的节点和边高亮**，可点击节点查看输入/输出
   - 决策表视图会标出**命中的规则行**
   - 面板下方显示最终结果 JSON 与耗时
4. 求值报错时，画布会标红出错节点并给出错误信息

> 模拟器走的是 Node 侧引擎（与 Python 同内核），模拟结果即为真实执行结果。

### 5.5 业务模式 / 开发模式

顶栏右侧切换：

- **业务模式（business）**：面向业务人员的简化视图，决策表更接近 Excel，隐藏技术细节
- **开发模式（dev）**：完整功能视图（表达式编辑器、函数编辑器、JSON Schema 等）

### 5.6 文件管理

| 按钮 | 说明 |
|---|---|
| **保存** | 当前规则写入 `rules/<文件名>.json`（有未保存修改时标题旁有黄点） |
| **新建** | 输入文件名创建新的规则文件（自动带输入→决策表→输出骨架） |
| **删除** | 删除当前规则文件（有确认提示） |

> 建议把 `rules/` 纳入 Git 管理：`git commit -m "VIP折扣 0.10 → 0.15"`，diff 清晰可审查。

## 6. ZEN 表达式语言要点

决策表单元格和表达式节点使用 ZEN 表达式语言。**本项目实际踩过的坑已标注 ⚠️**。

### 6.1 一元表达式（决策表输入单元格）

| 写法 | 含义 |
|---|---|
| `"vip"` | 等于 vip |
| `>= 500`、`< 20`、`!= 1` | 比较 |
| `[20..40]` | 区间（含边界） |
| `"US", "CA"` | 在列表中 |
| （空） | 匹配任意值 |

### 6.2 标准表达式（表达式节点 / 决策表输出）

```js
sum(map(items, #.price * #.qty))     // 数组聚合，# 引用元素
customer.tier == "vip" ? 10 : 20     // 三元
total > 1000 && level != "normal"    // 与或非：&& || !
```

### 6.3 字段引用规则 ⚠️

| 写法 | 引用对象 |
|---|---|
| `shippingFee`（裸字段名） | **上游节点传来的数据**（如决策表输出） |
| `$.finalFee` | **本节点内先算出的**变量（expressions 列表中靠前的 key） |

错误示例：决策表输出 `shippingFee`，表达式节点里写 `$.shippingFee` → 求值报错，应写 `shippingFee`。

### 6.4 严格类型 ⚠️

`+` 不会自动做类型转换，字符串拼接必须显式转换：

```js
"¥" + 0.5        // ❌ 报错: Opcode Add: Unsupported type
"¥" + string(0.5) // ✅ "¥0.5"
```

### 6.5 常用内置函数

`sum / min / max / map / filter / contains / string / number / upper / lower / size / date` 等，
完整参考：[ZEN Expression Language](https://docs.gorules.io/learn/zen-language/syntax)

## 7. Python 引擎使用

### 7.1 REST API（服务运行于 :8800）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/health` | 健康检查 |
| GET | `/api/decisions` | 列出所有规则文件 |
| GET | `/api/decisions/{name}/content` | 查看规则 JSON（name 可不带 .json 后缀） |
| POST | `/api/decisions/{name}/evaluate` | 求值，body: `{"context": {...}}` |
| POST | `/api/evaluate` | 按 key 求值，body: `{"key": "shipping-fee.json", "context": {...}}` |

**调用示例：**

```powershell
# PowerShell
$body = '{"context": {"customer": {"tier": "vip"}, "order": {"total": 800}, "cart": {"weight": 12}}}'
Invoke-RestMethod -Method Post -Uri http://localhost:8800/api/decisions/shipping-fee/evaluate `
  -ContentType "application/json" -Body $body
```

```python
# Python (requests)
import requests

resp = requests.post(
    "http://localhost:8800/api/decisions/shipping-fee/evaluate",
    json={"context": {"customer": {"tier": "vip"}, "order": {"total": 800}, "cart": {"weight": 12}}},
)
print(resp.json())
```

**返回结构：**

```json
{
  "performance": "248.1µs",
  "result": {
    "customer": { "tier": "vip" },
    "order": { "total": 800 },
    "cart": { "weight": 12 },
    "shippingFee": 0,
    "discount": 0.1,
    "finalFee": 0,
    "finalFeeText": "¥0"
  }
}
```

- `result`：决策输出。默认包含输入数据（passThrough）+ 各节点输出字段
- `performance`：本次求值耗时
- 求值失败返回 HTTP 400，detail 中含错误类型与出错节点

### 7.2 命令行（run.py）

```powershell
cd engine

# 直接给上下文 JSON
python run.py shipping-fee '{\"customer\":{\"tier\":\"vip\"},\"order\":{\"total\":800},\"cart\":{\"weight\":12}}'

# 从文件读上下文
python run.py customer-score @ctx.json

# 也可以直接指定规则文件路径
python run.py ..\rules\customer-score.json '{\"customer\":{\"years\":6,\"monthlySpend\":3000}}'
```

### 7.3 作为 Python 库嵌入（推荐生产用法）

```python
import zen
from pathlib import Path

RULES_DIR = Path("rules")

def loader(key: str) -> str:
    """按 key 加载规则文件，子决策引用也走这里"""
    return (RULES_DIR / key).read_text(encoding="utf-8")

engine = zen.ZenEngine({"loader": loader})

result = engine.evaluate("shipping-fee.json", {
    "customer": {"tier": "vip"},
    "order": {"total": 800},
    "cart": {"weight": 12},
})
print(result["result"]["finalFeeText"])   # ¥0
```

完整示例见 `engine/demo.py`（含批量求值、Switch 分流演示）。

### 7.4 规则热加载

引擎实例在创建时编译规则。要"不重启进程加载新规则"，重建 engine 即可：

```python
def reload_engine() -> zen.ZenEngine:
    return zen.ZenEngine({"loader": loader})

engine = reload_engine()   # 规则文件变更后调用
```

## 8. 典型工作流：改一条规则并上线

以"VIP 折扣从 0.10 调整为 0.15"为例：

1. **打开编辑器** http://localhost:8700，选择 `shipping-fee.json`
2. **修改规则**：双击「运费决策表」→ 把 `i1="vip", i2=">= 500"` 行的折扣列 `0.1` 改为 `0.15`
3. **模拟验证**：右侧模拟器 Run 几组数据（VIP 600 元 / VIP 400 元 / 普通用户），确认命中行和结果符合预期
4. **保存**：点击「保存」，`rules/shipping-fee.json` 更新（可 git commit 留痕）
5. **生效**：
   - REST/uvicorn 方式：重启 uvicorn，或按 7.4 重建 engine
   - 库方式：调用 `reload_engine()`
6. **验证**：`python run.py shipping-fee '{...}'` 或 curl 检查线上结果

## 9. 常见问题 FAQ

**Q1：编辑器能打开，但模拟器报"求值出错"？**
看画布上标红的节点：多数是表达式语法/类型问题（见第 6 节），尤其是 `$.xxx` 引用错对象、字符串拼接未用 `string()` 转换。

**Q2：改了规则并保存，Python 求值结果没变？**
引擎实例缓存了编译结果。重启 uvicorn 或重建 engine 实例（见 7.4）。

**Q3：pip 安装 zen-engine 失败（公司内网源）？**
加参数指定公开镜像源：
`pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/`

**Q4：PowerShell 里 curl/python 输出中文乱码？**
只是控制台显示编码问题（GBK vs UTF-8），返回的 JSON 数据本身是正确的。Python 脚本里可加：
`sys.stdout.reconfigure(encoding="utf-8")`

**Q5：端口被占用？**
`netstat -ano | findstr :8700` 找到 PID 后 `taskkill /PID <pid> /F`；
或换端口：编辑器 `set PORT=8701 && node server.mjs`（PowerShell: `$env:PORT=8701; node server.mjs`），引擎 `uvicorn app:app --port 8801`。

**Q6：decisionNode（子决策）怎么用？**
在一个规则里添加「Decision」节点，`content.key` 填另一个规则文件名（如 `shipping-fee.json`）。Python 侧的 loader 会按 key 加载对应文件。注意：编辑器模拟器目前按单文件求值，跨文件子决策请在 Python 侧验证。

**Q7：决策表 hitPolicy 怎么选？**
`first`：命中第一行就返回（条件互斥的常规场景）；`collect`：收集所有命中行（如打标签、累计加分的场景）。另外输出列字段名以 `[]` 结尾（如 `tags[]`）可单列做收集。

**Q8：想把规则存数据库/配置中心而不是文件？**
Python loader 是普通函数，改成查库即可：
```python
def loader(key: str) -> str:
    return db.query("SELECT content FROM rules WHERE key=%s", key)
```
编辑器侧如需对接，改造 `editor/server.mjs` 中的文件读写接口即可。
